import * as React from "react";
import styles from "../../Styles/Atoms/bottomLine.module.css"
export const BottomLine = (props: any) => {
    return <div className={styles.bottomLine}>

    </div>;
};
